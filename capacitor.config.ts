import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.hanginlearn', // change to your reverse-domain id
  appName: 'HangInLearn',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
